{
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "allowImportingTsExtensions": true,
        "lib": ["ES2020", "DOM", "DOM.Iterable"],
        "types": ["node"]
    },
    "include": ["tests/**/*.ts", "src/**/*.ts", "src/**/*.d.ts"],
    "exclude": ["node_modules", "dist", "dev", "vendor"]
}
