# NextAction 领域上下文

本文件定义 NextAction 的稳定领域语言。它描述任务、项目和行动之间的概念关系；具体属性键、RPC、缓存和 UI 规则记录在项目设计文档或 ADR 中。

## 任务与关系

**任务（Task）**：NextAction 管理的可执行或可组织工作项，底层对应一个思源块。

**原生任务**：思源列表结构中带有任务标记的任务。任务身份是列表项，而不是列表项内的文字子块。

**文档任务**：以思源文档块作为任务身份的任务。Project 是文档任务的一种特殊领域身份。

**结构父任务**：根据思源文档树自然推导出的最近任务祖先。它只表达笔记结构，不一定表达项目归属。

**有效父任务**：任务当前采用的逻辑父级。用户明确指定的父级优先；没有明确指定时，才使用结构父任务。清空明确指定的父级后，系统回到结构父任务规则。

## 项目与行动

**Project（项目）**：一个由个人负责推进、具有明确预期结果并且可以结束的承诺。Project 锚定在思源文档块上，但不等同于“拥有子任务的父任务”。

**Action（行动）**：为推动 Project 结果发生而执行的一次具体工作。Action 可以位于任意思源位置，通过有效父任务归属于 Project。

**Stage（阶段）**：带有阶段标记的 Action。Stage 可以有子 Action，也可以自己执行；它仍使用普通 Action 的状态、依赖、日期和 Next Action 语言。

**Outcome（预期结果）**：Project 完成后要取得的结果，用一句话描述“完成后会得到什么”。

**Definition of Done（完成判定，DoD）**：用户关闭 Project 前要人工确认的完成条件。DoD 是说明文本；需要逐项追踪的条件应建立为 Action。

**Next Action（下一步行动）**：当前具备执行条件、能够推动结果前进的 Action。它由系统根据可执行性证据推导，用户负责选择优先级。

**Project Review（项目回顾）**：对 Project 的一次重新校准，检查 Outcome、DoD、计划、Next Action、等待/阻塞和项目状态。

**Project Support（项目支持材料）**：与 Project 相关的研究、方案、决策记录和参考资料。它是项目上下文，不是 Action，也不参与项目完成判定。

## 项目边界

- Project、Action、Stage 是不同的领域语义；Stage 不是新的生命周期对象。
- Project 使用现有任务状态表达生命周期，不另造一套状态名。
- `custom-na-parent` 在产品语义上表示统一的逻辑父关系：指向普通任务时是父任务，指向 Project 时是项目归属。
- Action 的逻辑归属不要求改变它在思源文档树中的物理位置；“加入项目”和“移动到项目文档”是不同动作。
- 项目进度以叶子 Action 为基础；父节点和 Stage 的状态不能与其叶子工作重复计数。
