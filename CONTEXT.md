# 任务身份上下文

本文件定义 NextAction 对“一个任务究竟是哪一个思源块”的统一解释。缓存、创建、转换、更新、父任务查找和编辑器必须遵守同一套规则。

## 原生任务

思源原生任务有两种等价结构：列表项自身带有任务标记，或者列表项直属的列表带有任务标记。两种结构都必须被识别为原生任务。

## 正式任务 ID

- 原生任务：列表项 ID。
- 文档任务和项目：文档 ID。

正式任务 ID 用于缓存、RPC、同步、状态更新和任务之间的引用。原生任务的文字子块 ID 不能代替正式任务 ID。

## 属性宿主

- 原生任务：列表项本身。
- 文档任务和项目：文档本身。

所有 `custom-na-*` 属性都写到属性宿主。原生任务的文字子块不承载 NextAction 任务属性。

## 内容块

原生任务列表项直属的第一个段落或标题块是内容块。它只用于读取标题、跳转和重命名。缺少内容块属于结构异常，但任务仍进入缓存；只有确实依赖内容块的操作（例如重命名）才返回原有错误。

## 结构父任务

从任务向上查找时，最近的原生任务列表项或文档任务是结构父任务。查找允许跨过普通列表、普通列表项和段落，但到达当前文档边界后必须停止，不能把另一个文档中的任务认作父任务。循环或损坏的祖先链也必须安全停止。

## 有效父任务

非空的 `custom-na-parent` 是人工指定的父任务，优先于结构父任务。该属性为空或未设置时，回退到结构父任务。

当前模型不区分“未设置父任务”和“人工设为无父任务”：两者都是空值。因此，缩进任务在重新加载后仍会采用结构父任务。

## 项目限制

项目只允许使用文档块。原生任务和其他块不能转换为项目；违反限制时继续使用现有错误码和错误语义。
