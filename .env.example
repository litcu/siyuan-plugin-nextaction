# 本地配置模板：复制为被 Git 忽略的 .env.local，再填写真实值。禁止提交真实凭据。

# 思源工作空间 data/plugins 目录的绝对路径，供 pnpm run release 部署使用。
SIYUAN_PLUGINS_DIR=C:\path\to\siyuan-workspace\data\plugins

# 思源访问授权码（新版本界面称“锁屏密码”），供 Docker 启动或浏览器 /check-auth 登录使用。
# 它不是 API token，不能放进 Authorization: Token 请求头。
SIYUAN_ACCESS_AUTH_CODE=replace-with-access-auth-code

# 在“设置 - 鉴权 - API token”中查看，供真实内核与 MCP 集成测试调用思源 API。
SIYUAN_API_TOKEN=replace-with-api-token
