# 更新日志

本文件记录每个版本面向用户的主要变更。发布前，请在 `[Unreleased]` 下按分类填写至少一条列表项；发布脚本会自动生成对应版本章节，并重建空白的 `[Unreleased]` 模板。

## [Unreleased]

<!-- 在下面至少一个分类中填写以 "- " 开头的更新内容。 -->

### 新功能

- 项目视图新增层级与看板模式，可按项目阶段浏览任务并进行项目结构规划；看板选择和项目层级关系会在切换后保持。
- 为任务属性补充帮助提示，并将 Action 类型限制在当前项目及其后代项目范围内，减少误操作。

### 优化

- 统一项目成员关系和任务条目物化逻辑，提升项目视图、任务缓存及相关操作的数据一致性。
- 完成 Svelte 5 公共组件契约迁移，统一组件生命周期与浏览器测试环境，降低前端运行时兼容风险。

### 问题修复

- 修复项目移动撤销时的结构保护、国际化提示及部分写入失败场景，避免撤销后项目层级错误。
- 修复项目视图阶段归类、层级布局和默认选择问题；同时改善任务面板及主题界面的文字对比度，并抑制原生任务复选框的主题干扰。
- 修复任务状态、转换和项目移动等操作成功后重复显示通知的问题，保留警告和错误提示。

### 兼容性说明

- 完成 Svelte 5 公共组件 API 迁移并移除临时 `componentApi: 4` 兼容层；发布包不再依赖 Svelte 内部运行时别名。运行插件至少需要思源 3.8.0；从源码构建需要 Node.js 22 或更高版本。开发命令使用 Vite 构建监听，不会默认启动对外可访问的开发服务器。

## [0.8.0] - 2026-08-26

### 新功能

- 新增面向结果的项目工作流：支持项目 Outcome、DoD、Action 与 Stage，编辑项目定义、规划阶段和行动层级，并查看项目进度、健康度与完成候选。
- 新增项目级回顾，将进度、风险、下一步行动、等待中和阻塞工作汇总为可逐项处理的检查队列；可从项目或回顾中直接创建下一步行动。
- 支持从源笔记提取 Action、批量确认 AI 提议，以及查看项目支持资料（正向引用与文档反向链接）。
- 支持在项目文档之间移动原生 Action，移动前预览结构变化，并提供可验证的一次性撤销。

### 优化

- 统一项目摘要、风险排序和控制状态，项目视图、回顾、MCP 与 AI 上下文使用同一套领域数据；项目计划支持创建、重命名、转换、重挂父级和手动排序。
- 项目定义编辑支持按字段保存、取消、重试和外部更新冲突处理，切换项目或视图时保留草稿并恢复编辑焦点。
- 移动 Action 时串行保护结构写入并回读权威状态，改进异步保存、导航返回、选择状态和失败反馈，降低索引延迟造成的不一致。

### 问题修复

- 修复项目定义保存完成或失败后编辑器重新挂载时状态未发布、草稿丢失或重试入口消失的问题。
- 修复文档反向链接方向识别、项目回顾返回后的滚动位置，以及风险行动创建后回顾数据未刷新的问题。
- 修复 Action 移动撤销在属性变化、目标位置漂移或部分写入失败时可能恢复错误结构或缓存的问题。

### 兼容性说明

- 新增的项目属性（Outcome、DoD、Action/Stage 类型）与现有 `custom-na-*` 任务属性共存；升级不需要迁移既有任务数据，项目正文仍保留为普通思源内容。

## [0.7.0] - 2026-08-23

### 新功能

- 思源原生任务列表项现可作为 NextAction 任务使用：创建、转换、状态更新、重复任务、“我的一天”、任务关系与 MCP 操作均使用统一的任务身份。
- 任务详情弹窗现支持直接创建子任务，并在抽屉、Dock 和编辑器入口之间保持一致的导航、保存与关闭行为。

### 优化

- 任务写入统一经过串行锁、权威回读、缓存更新和变更广播，减少重复通知、中间状态以及写入失败后的界面不一致。
- 优化大量任务下的增量同步与层级更新，以单次分组重建父子关系，并避免行内子孙节点变化触发不必要的全量缓存重建。
- 任务详情在打开期间会同步权威更新、字段级草稿合并和删除结果，切换任务或关闭时也会保护未保存的编辑。
- 多选搜索控件在窄面板中可正确收缩和换行，避免 Context 与标签内容横向溢出。
- MCP 状态会在保存设置后立即刷新，无效收件箱或日记目标会保留具体原因并引导到对应设置。

### 问题修复

- 修复嵌套原生任务的祖先、父子关系和编辑器状态图标可能归属错误的问题，子任务现保留自己的六态图标与任务身份。
- 修复创建、移动或删除子任务后，已打开详情与父任务层级未及时刷新的问题。
- 修复重复任务完成当前周期并推进到下一周期后，“我的一天”完成标记丢失的问题。
- 替换无声的提醒占位音频，五种通知音现均可正常播放且具有可区分的听感。

### 兼容性说明

- 最低思源版本保持 3.8.0；从 0.6.0 升级不需要迁移现有任务数据。
- 已移除 0.6.0 正式发布前的旧设置、任务属性迁移与 V1 同步回退；如仍在使用预发布测试数据，需按当前格式重新建立。

## [0.6.0] - 2026-08-16

### 新功能

- 项目工作区新增自适应甘特图，支持日、周、月三种时间尺度，并展示项目层级、任务依赖、里程碑、汇总进度和未排期任务；项目视图也会使用更明确的项目专属文案和操作入口。
- 任务同步新增带修订号的增量协议，可按最终状态合并连续变更，并在缓存重建、修订缺口或运行实例变化时自动恢复完整快照，减少全量刷新带来的额外开销。
- MCP 工具现通过思源 Agent 能力接口注册，并明确标注本地读取或写入权限，便于宿主统一展示和管理全部 14 个任务工具。

### 优化

- 优化项目层级与规划体验：支持在文档项目下创建子项目并可靠保存父子关系，项目编辑后会立即刷新，进行中的子任务会计入项目进展而不再被误判为阻塞。
- 集中维护父子关系、依赖关系、顺序项目和派生状态索引；前端按批次归并任务变更并只重算一次统计信息，提升大量任务下的同步效率与状态一致性。
- 补齐 AI、提醒、错误提示和通用控件中的中英文文案，并统一任务写入失败的错误分类与提示，保留可重试的用户输入。
- 优化窄面板中的项目筛选布局和下拉选择器在不同主题下的文字显示，避免控件过度堆叠或选项内容不可见。

### 问题修复

- 修复重复任务跳过、推进或恢复后“我的一天”状态不同步的问题；移除已持久化条目时会立即通知前端，并清理过期的完成标记。
- 修复任务状态变化后回顾列表和展开的检查分组未及时刷新的问题。
- 修复设置面板因存在未保存改动而阻止首次关闭后，后续点击遮罩无法再次发起关闭的问题。
- 修复刚创建文档块后立即修改标题时，因 SQL 索引尚未刷新而偶发失败的问题。
- 修复部分英文界面仍显示中文，以及部分 AI、提醒和错误提示缺少本地化的问题。

### 兼容性说明

- 最低思源版本提升至 3.8.0，以使用新的 Agent 能力接口；升级前请先确认思源版本满足要求。
- 本次升级不需要迁移现有任务数据；新版增量同步仍保留旧任务变更通知作为兼容回退。
- 支持范围为桌面端、桌面浏览器和移动浏览器；思源原生 iOS、Android 客户端仍不在当前支持范围内。

## [0.5.1] - 2026-08-13

### 优化

- 设置面板的主操作改为“保存”，成功保存后保持面板打开，便于继续调整；关闭未保存设置、重置配置及执行维护操作时会给出明确的二次确认。
- 项目视图会根据面板实际宽度重排进度指标和操作区，在窄面板中保持操作按钮完整可用。

### 问题修复

- 修复顺序项目、子任务及依赖任务在状态变化或重排后，阻塞状态和项目排序未及时同步到界面的问题。
- 修复已逾期任务同时出现在回顾检查清单和到期列表中的重复展示，并在存在逾期任务时突出显示逾期数量。

## [0.5.0] - 2026-08-09

### 新功能

- 任务面板新增“新建任务”入口，可直接创建任务或项目，并设置状态、优先级、开始/截止时间、上下文、标签和备注；项目视图及任务详情中也可快速新建子任务。
- 新建任务支持保存到配置的收件箱、当天日记或指定文档，并可选择文本块或文档块形式；新增可搜索的文档选择器，项目会创建为所选文档下的子文档。
- 移动端 Dock 可在精简侧栏与完整任务面板之间切换，在手机和平板上也能访问全部任务视图与详情操作。

### 优化

- 将任务创建的默认存储位置统一移至“常规”设置，由任务面板和 MCP 共用；原 MCP 中的收件箱文档、日记笔记本等配置会自动迁移。
- “我的一天”现始终可用，导航、编辑器菜单和任务详情中的相关入口保持一致，不再因开关配置而隐藏。
- 统一回顾到期与逾期判断：回顾徽标会同时统计需要回顾和已经逾期的任务并自动去重，标记已回顾后立即同步界面，带时间的截止日期也会按实际时刻判断。
- 优化任务面板、Dock 与任务详情的紧凑布局和窄屏适配；改进提示气泡、日期选择器、搜索下拉框和拖拽浮层的主题、定位与层级表现。

### 问题修复

- 修复任务刚创建或缓存尚未同步时立即修改属性可能被误判为无效块、导致更新失败的问题。
- 修复 MCP 以列表形式创建任务时，任务属性可能写入列表容器而非实际文本块，进而造成缓存异常或后续操作失败的问题。
- 修复紧凑任务卡片无元数据时标题未居中、空元数据行仍占位，以及编辑器状态标记对齐不一致的问题。
- 修复任务任务属性面板错误提示被遮挡问题，现固定显示在详情内容顶部。
- 修复抽屉、日期/搜索弹层、拖拽预览、通知和提示气泡之间的遮挡与层级冲突，并避免点击后残留的提示气泡覆盖任务详情。

### 兼容性说明

- 插件现已声明支持桌面端和移动端浏览器，可在思源 Web 前端中加载使用。
- 项目许可证已由 MIT Commons Clause 调整为 PolyForm Noncommercial 1.0.0；非商业用途可按新许可证使用，商业用途需要另行获得授权。
- 本次升级不需要迁移任务数据；旧版 MCP 任务创建位置设置会在加载时自动兼容到新的通用任务创建设置。

## [0.4.1] - 2026-08-07

### 新功能

- 已完成任务支持分页浏览，并可按完成时间、优先级分数、截止日期、重要性或手动优先级排序。
- MCP 工具升级为统一的批量增删改查接口，单次可处理最多 100 个任务，并逐项返回成功或失败结果。
- MCP 任务更新支持标题、任务/项目类型、状态和重复规则；同时支持批量转换思源块及调整“我的一天”。

### 优化

- 任务缓存改为游标分页加载，避免大型任务库受思源 SQL 单次返回数量限制而漏载任务。
- 优化“全部任务”的树形列表构建和任务索引，提升大量任务下的展示性能，并保证孤立或循环父子关系中的任务仍可见。
- 发布流程现在会校验并自动整理结构化更新日志，GitHub Release 同步展示本次更新内容和完整变更链接。

### 问题修复

- 修复任务筛选搜索框无法连续输入、输入内容可能被旧状态覆盖的问题。
- 修复从编辑器打开任务详情时缓存尚未就绪会静默失败的问题；现在会重建缓存、重试并显示明确错误。
- 修复搜索框和链接输入控件的重复焦点轮廓及部分状态图标对齐问题。

### 兼容性说明

- MCP 工具已合并并重命名为 `search_tasks`、`get_tasks`、`create_tasks`、`update_tasks`、`delete_tasks`、`convert_blocks_to_tasks` 和 `update_my_day` 等批量接口；使用旧工具名的 MCP 客户端或提示词需要同步调整。
- 本次升级不需要迁移现有任务数据；MCP 的 `delete_tasks` 只移除 NextAction 任务身份，不删除原始思源块内容。
